# 片库整理说明

本目录目前更像是一个“做片工作库”，不是单纯的视频成片库，主要由以下几部分组成：

## 当前分区

- `脚本片型参考库.md`
  - 片型分类总表，适合先看这个文件找参考方向。
- `film-pipeline/`
  - 主流程技能与参考资料。
- `step-1-film-director/` 到 `step-5-director-prompts/`
  - 分步骤技能目录，对应从定调到生成提示词的完整流程。
- `film-pipeline-skills/`
  - 技能打包版/分发版内容。
- `20260317_internal-mg-version/`
  - 一套完整示例产出，可作为模板参考。
- `send-to-xx/`、`subagent-shortcuts/`、`xx-shortcuts/`
  - 快捷插件或辅助脚本。
- `Deploy-Film-Pipeline-To-NAS.ps1`、`Restore-OpenClaw.ps1`、`nas_openclaw*.json` 等
  - 部署、恢复、NAS、OpenClaw 相关运维文件。
- `_workspace_tmp/`
  - 已集中收纳的临时文件、抽帧结果、测试脚本、临时导出数据。

## 本次整理内容

- 已把所有顶层 `tmp_*` 文件和目录统一移动到 `_workspace_tmp/`
- 未改动主流程目录名，避免影响现有引用关系
- 未删除任何文件，只做归拢

## 后续建议

- 日常临时测试继续统一放进 `_workspace_tmp/`
- 新的项目成稿建议单独建目录，例如 `projects/项目名/`
- 如果要继续精细整理，下一步可以把顶层运维脚本再归到 `ops/` 或 `tools/`

